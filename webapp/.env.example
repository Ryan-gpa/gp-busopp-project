# Set this in Vercel → Project Settings → Environment Variables
# Point it at wherever you deploy the FastAPI backend (Railway, Render, Fly.io, etc.)
# Leave blank to use relative /api/* (only works when backend is on the same host)
VITE_API_URL=https://your-backend.railway.app
APOLLO_API_KEY=
